# from django.forms import ModelForm, widgets, Select
# from .models import CustomerAddress

# class CustomerAddressForm(ModelForm):
#     class Meta:
#         model = CustomerAddress
#         fields = '__all__'
#         widgets = {
#             "divisions":Select(attrs = {'class':'input-field'}),
#             "districts":Select(attrs = {'class':'input-field'}),
#             "upazilas":Select(attrs = {'class':'input-field'}),
#             "unions":Select(attrs = {'class': 'input-field'})
#         }

